# VNFest 2.2.0 更新日志

发布日期：2026-09-13

统计范围：2026-09-03 至 2026-09-13

适用对象：VNFest 用户、同好会负责人、管理员与维护者

VNFest 2.2.0 把“发现同好会”之后的社区互动继续向前推进：同好会动态迁移到 React/Vite，搜索成为独立入口，互相关注的用户可以发送私信；同好会管理后台也迁移为 React 工作台。账号安全、GalgameTool、动态图片、Tier 表和后端数据结构同步完成了一轮整理。

网站使用文档中的同版历史记录：<https://www.map.vnfest.top/wiki/guide/#/updates/2-2-0>

## 同好会动态

- 同好会动态的首页、搜索、个人空间、消息、动态详情统一使用 React/Vite 路由和响应式布局。
- 旧论坛入口改为归档提示，引导用户回到同好会动态发布和浏览内容。
- 新增独立“搜索”入口，可搜索动态和用户；桌面左侧导航与移动端底部导航保持一致。
- 发布动态时支持多图片选择、预览、网格展示和手动裁剪；动态图片使用自由比例裁剪。
- 个人资料横幅支持 3:1 裁剪并输出 1200×400 图片。
- 关联同好会改为居中弹层，支持本地搜索、成员身份展示、取消关联和选中反馈。

## 好友与私信

- 双方互相关注后自动形成好友关系，只有好友之间显示“发私信”入口。
- 新增会话列表、好友入口、按日期分组的聊天气泡、已读状态和未读数量。
- 未读数量每 8 秒刷新，会话内容每 5 秒增量拉取。
- 消息正文最长 1000 字；发送频率限制为每 10 分钟 30 次。
- 聊天支持图片附件、发送前预览和点击放大查看。
- 修复图片灯箱被顶部用户名和底部 sticky 输入框遮挡的问题：灯箱现在挂载到页面顶层，覆盖完整视口并保持居中；图片最大宽度为 82vw/680px，最大高度为 72vh，在手机和桌面端均保留安全边距。

## 同好会管理后台

- 同好会管理后台迁移为 React/Vite 工作台。
- 保留待审核、外交、已通过、成员、设置、绑定码、机器人 Token、推荐位、企划、认可凭证、江苏和用户等既有权限边界。
- 桌面端提高信息密度，移动端改为抽屉导航。
- 推荐位继续固定为 12 个位置；桌面端使用拖拽排序，移动端使用点击换位。
- 成员、企划、推荐位和认可凭证补充契约测试与高数据量场景验证。

## 登录与账号安全

- QQ/Discord 登录补充账号凭证升级流程。
- 第三方登录用户可以完成邮箱验证码、设置密码、凭证完成状态和账号提供商转移。
- 数据库结构增加 `credentials_completed_at` 和 `oauth_account_challenges`。
- 邮箱唯一性、PHP Session 和现有登录/注册/API 契约保持不变。
- 用户中心同步整理账号安全、通知筛选和同好会动态入口。

## GalgameTool

- GalgameTool 扩展为履历书、MEME 看板和 Tier 表三个视图。
- Tier 表支持卡片搜索、拖拽排序、行与颜色编辑、本地图片卡、JSON 导入导出和 PNG 导出。
- 登录用户获得云同步入口。
- MEME/Tier 导出补充进度提示、图片预加载和颜色兼容处理。
- 生成结果可以转发到同好会动态。
- 原有 Bangumi、VNDB、CnGal 等数据源和移动端编辑入口继续保留。

## 接口与数据结构

- 新增或整理动态、动态图片、用户横幅、私信和 Tier 表接口。
- Posts/DM schema 接入统一迁移脚本。
- 私信会话按用户 ID 规范化为唯一组合；消息、附件和未读索引分别维护。
- 清理旧专栏 API、旧论坛/专栏辅助文件和重复构建资源，同时保留归档入口与现行 Column 构建产物。
- 本地迁移检查覆盖 SQLite 与 MySQL 的建表和补列分支。

## 其它前端和工程更新

- 新增间谍游戏的 React 桌面/移动端界面原型和静态构建；当前仍作为独立原型保存，不表示已接入主站业务流程。
- GitHub Pull Request 镜像检查补充 Buildx 初始化，解决 GitHub Actions 缓存无法由 Docker 默认驱动导出的问题。
- OAuth、管理员工作台、同好会动态、Wiki 更新记录和图片灯箱均补充对应的契约或浏览器回归检查。

## 验证结果

- Column React 构建通过。
- Column 动态契约测试通过。
- Electron 浏览器检查覆盖 4 个视口 × 9 个路由，共 36 个场景。
- 私信图片灯箱专项回归通过，确认灯箱直接位于 `document.body`、使用固定定位、层级为 `1000`，能够覆盖顶部信息条和底部输入区并居中显示。
- Wiki 使用文档中日双语结构、顺序、文章链接和种子同步检查通过。
- OAuth 账号流程和管理员 React 契约检查通过。
- 完整 `npm run check` 通过。

## 发布状态与边界

- 2.2.0 源码与更新记录已发布到 GitHub 主仓库。
- 图片灯箱对应的 `/column/` 入口和哈希 JS/CSS 已通过受控 SSH 流程发布到生产环境；服务器文件哈希、权限、入口引用和公网 HTTP 200 回读均已确认。
- 旧的哈希资源保留在服务器上，以兼容仍在使用旧 HTML 缓存的客户端。
- 文档发布和 GitHub 提交不会自动执行生产数据库迁移。其余 2.2 数据库变更仍须在目标服务器先备份数据，再运行迁移并核对 `posts`、`dm_conversations`、`dm_messages`、OAuth 挑战表等结构。
- QQ/Discord 的真实授权、邮件实际投递、两个互关账号之间的线上收发，以及登录管理员的真实业务操作需要相应账号或运营授权；本日志不会把静态检查写成这些场景已经完成。

## 升级提示

1. 先备份生产数据库、运行时数据和待替换文件。
2. 核对服务器当前文件哈希，防止覆盖并发修改。
3. 按需执行 `scripts/migrate.php`，随后核对新增表、字段、唯一索引和未读索引。
4. 发布 React/Vite 页面时同时上传入口 HTML 和它引用的哈希 JS/CSS，不要删除旧哈希资源。
5. 使用无缓存请求核对公开页面、资源 MIME 类型和关键标记，再进行登录态人工验收。
